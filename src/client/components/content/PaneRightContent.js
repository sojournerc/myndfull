
import React from 'react';
import cn from 'classnames';
import create from '../componentFactory';

export default create({
  displayName: 'PaneRightContent',
  propTypes: {
  },
  render() {
    return <div id="PaneRightContent" className={cn(
    )}>
    </div>
  }
});
